# Example only: configure authentication and durable storage before deployment.
# Local-first defaults intentionally do not include keys or a secret resource name.
command:
  - "streamlit"
  - "run"
  - "app.py"
env:
  - name: EMBEDDING_PROVIDER
    value: "local"
  - name: CHAT_PROVIDER
    value: "none"
